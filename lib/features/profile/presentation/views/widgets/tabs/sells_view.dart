import 'package:flutter/material.dart';

class SellsView extends StatelessWidget {
  const SellsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child:  Text("sells View"),);
  }
}