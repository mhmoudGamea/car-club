import 'package:flutter/material.dart';
class CarCenterLocationBody extends StatelessWidget {
  const CarCenterLocationBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Car Centers Location"),
    );
  }
}
